# Only needed for live LLM extraction (the pipeline ships with cached
# extractions, so everything runs without this).
ANTHROPIC_API_KEY=

# Per-run cost ceiling for extraction jobs. Jobs abort rather than exceed it.
LLM_BUDGET_USD=2.00
